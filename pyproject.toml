[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "teruo — inventory agent CLI for kebab food trucks"
requires-python = ">=3.11"
dependencies = [
    "strands-agents==1.54.0",
]

[dependency-groups]
dev = [
    "matplotlib>=3.11.1",
]
